<resources>

    <style name="AppTheme" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorPrimaryDarker</item>
        <item name="android:colorBackground">@color/colorPrimaryDarker</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="overlapAnchor">true</item>
    </style>

    <style name="AppTheme.DialogOverlay" parent="ThemeOverlay.AppCompat.Dialog">
        <item name="android:background">@color/colorDialog</item>
    </style>

    <style name="AppTheme.SourceListOverlay" parent="ThemeOverlay.AppCompat">
        <item name="colorControlActivated">@color/colorSelectedListItem</item>
    </style>

    <style name="AppTheme.NoActionBar.BlackWindow" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
    </style>
</resources>
